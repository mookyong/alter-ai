version: 2

models:
  - name: stg_customers
    description: PostgreSQL customers staging model
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
  - name: stg_orders
    description: PostgreSQL orders staging model
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
