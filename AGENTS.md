# AGENTS.md

- This repo is documentation for the DataStage -> dbt/Airflow conversion project.
- Keep active docs in the numbered root folders: `00_overview` through `09_minutes`.
- Use `99_archive` for completed or retired documents.
- Trust the root `README.md` for folder purpose and placement.
- `01_planning/산출물 정의표.md` is the source of truth for official vs working vs reference deliverables.
- Preserve the existing naming style: numbered folders, descriptive Korean filenames, and date/version suffixes when useful.
- There are no repo-local build, test, lint, or typecheck commands defined here.
- `.vscode/settings.json` opens `csv`, `tsv`, `parquet`, and `xlsx` files in the DuckDB viewer in VS Code.
